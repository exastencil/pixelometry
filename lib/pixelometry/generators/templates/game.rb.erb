# Load Ruby2D and Pixelometry
require 'pixelometry'

# Set up the structure of our game
define_game title: '<%= app_name.humanize %>' do
  # World
  create_scene do
    # Create entities for your game here
  end

  # UI
  create_scene renderer: ScreenSpaceRenderer do
    # Create any UI for your game here
    create_entity :label, text: 'Edit game.rb to get started', x: 200, y: 200, opacity: 0.8
  end
end